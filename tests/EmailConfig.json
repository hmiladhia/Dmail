{
	"email": {
	"mail_server": "${os_environ[server]}",
	"mail_port": 587,
	"sender_email": "${os_environ[user]}",
	"sender_credentials": "${os_environ[password]}",
	"mail_use_tls": true
	},
	"receiver": "${os_environ[receiver]}"
}